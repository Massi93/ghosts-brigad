{
  "name": "das-shop",
  "private": true,
  "version": "0.1.0",
  "description": "das-shop — multi-platform shopping (Web + Android, single Next.js + libSQL backend)",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "start": "npm --workspace apps/web run start"
  }
}
